{
  "name": "casino-intelligence-platform",
  "version": "1.0.0",
  "description": "Casino Intelligence Platform - Enterprise demonstration",
  "private": true,
  "scripts": {
    "dev": "npm run dev:frontend",
    "dev:frontend": "cd frontend && npm install && npm run dev",
    "dev:backend": "cd backend && npm install && npm run dev",
    "dev:both": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "start": "cd frontend && npm run build && cd ../backend && npm run build && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["casino", "intelligence", "platform", "seo", "ai"],
  "author": "Casino Intelligence Team",
  "license": "MIT"
}